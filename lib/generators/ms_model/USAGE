Description:
    Generates a new Me Salva! customized model at app/models.
    Pass the model name as an argument, either CamelCased or snake_cased.
    It is possible to pass the attributes arguments as well.

Example:
    `bin/rails generate ms_model Awesome`
    `bin/rails generate ms_model Awesome attribute1:string attribute2:integer attribute3:references`

    creates a Me Salva! customized awesome model:
        app/models/awesome.rb
